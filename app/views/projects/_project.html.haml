.project{id: project.slug}
  .project-image-wrapper
    = link_to image_tag("projects/#{project.slug}.jpg", class: 'img-responsive', alt: project.name), contact_path

  %h3= project.name
  %p= project.description
